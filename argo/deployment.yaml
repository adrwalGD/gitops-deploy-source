apiVersion: apps/v1
kind: Deployment
metadata:
  name: my-web-app
spec:
  replicas: 1
  selector:
    matchLabels:
      app: my-web-app
  template:
    metadata:
      labels:
        app: my-web-app
    spec:
      # Reference the image pull secret so Kubernetes can authenticate to the registry
      imagePullSecrets:
      - name: acr-secret
      containers:
      - name: my-web-app
        resources:
          requests:
            memory: "128Mi"
            cpu: "250m"
          limits:
            memory: "256Mi"
            cpu: "500m"
        image: adrwalgitopslhfv.azurecr.io/my-web-app:921be5cb9f5b12987bcf047ae422acbad7065448
        imagePullPolicy: Always
        ports:
        - containerPort: 3001
